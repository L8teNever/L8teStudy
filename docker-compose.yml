services:
  web:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./instance:/data
    environment:
      - DATABASE_URL=sqlite:////data/l8testudy.db
      - SECRET_KEY=${SECRET_KEY:-your-default-dev-secret}
      - UPLOAD_FOLDER=/data/uploads
    restart: always
